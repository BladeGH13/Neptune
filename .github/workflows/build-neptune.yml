name: Build Neptune Browser (Windows)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-windows:
    name: Build for Windows 10/11
    runs-on: windows-2022

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────────────
      - name: Checkout Neptune
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ── 2. Set up Python ───────────────────────────────────────────────────
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ── 3. Add NSIS (pre-installed on runner) to PATH ──────────────────────
      - name: Add NSIS to PATH
        shell: powershell
        run: |
          $nsisPath = "C:\Program Files (x86)\NSIS"
          if (Test-Path $nsisPath) {
            echo "Found NSIS at $nsisPath"
            echo $nsisPath | Out-File -Append -Encoding ascii $env:GITHUB_PATH
          } else {
            $alt = "C:\ProgramData\chocolatey\lib\nsis\tools"
            echo "Trying fallback: $alt"
            echo $alt | Out-File -Append -Encoding ascii $env:GITHUB_PATH
          }
          makensis /VERSION

      # ── 4. Install Mozilla Build ───────────────────────────────────────────
      - name: Install Mozilla Build
        shell: powershell
        run: |
          $url = "https://ftp.mozilla.org/pub/mozilla/libraries/win32/MozillaBuildSetup-Latest.exe"
          $out = "$env:TEMP\MozillaBuildSetup.exe"
          Invoke-WebRequest -Uri $url -OutFile $out
          Start-Process -FilePath $out -ArgumentList "/S" -Wait

      # ── 5. Write mozconfig ─────────────────────────────────────────────────
      - name: Write mozconfig
        shell: powershell
        run: |
          $mozconfig = @"
          ac_add_options --enable-application=browser
          ac_add_options --enable-optimize
          ac_add_options --disable-debug
          ac_add_options --disable-tests
          ac_add_options --disable-accessibility
          ac_add_options --disable-webrtc
          ac_add_options --disable-crashreporter
          ac_add_options --disable-updater
          ac_add_options --disable-maintenance-service
          ac_add_options --with-branding=browser/branding/unofficial

          mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/obj-neptune
          mk_add_options AUTOCLOBBER=1
          "@
          $mozconfig | Out-File -FilePath "mozconfig" -Encoding utf8
          echo "mozconfig written:"
          cat mozconfig

      # ── 6. Cache build objects ─────────────────────────────────────────────
      - name: Cache build objects
        uses: actions/cache@v4
        with:
          path: obj-neptune
          key: neptune-obj-${{ runner.os }}-${{ hashFiles('mozconfig') }}
          restore-keys: neptune-obj-${{ runner.os }}-

      # ── 7. Bootstrap (using Mozilla Build bash directly) ───────────────────
      - name: Bootstrap
        shell: powershell
        run: |
          $bash = "C:\mozilla-build\msys\bin\bash.exe"
          $repo = $env:GITHUB_WORKSPACE -replace '\\', '/'
          $repo = $repo -replace '^([A-Za-z]):', '/\1'
          & $bash --login -c "cd '$repo' && python3 ./mach bootstrap --application-choice=browser --no-interactive"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # ── 8. Build (using Mozilla Build bash directly) ───────────────────────
      - name: Build
        shell: powershell
        run: |
          $bash = "C:\mozilla-build\msys\bin\bash.exe"
          $repo = $env:GITHUB_WORKSPACE -replace '\\', '/'
          $repo = $repo -replace '^([A-Za-z]):', '/\1'
          & $bash --login -c "cd '$repo' && python3 ./mach build"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
        env:
          MACH_NO_TERMINAL_FOOTER: 1

      # ── 9. Package ─────────────────────────────────────────────────────────
      - name: Package
        shell: powershell
        run: |
          $bash = "C:\mozilla-build\msys\bin\bash.exe"
          $repo = $env:GITHUB_WORKSPACE -replace '\\', '/'
          $repo = $repo -replace '^([A-Za-z]):', '/\1'
          & $bash --login -c "cd '$repo' && python3 ./mach package"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      # ── 10. Upload installer ───────────────────────────────────────────────
      - name: Upload Neptune installer
        uses: actions/upload-artifact@v4
        with:
          name: Neptune-Windows-Installer
          path: obj-neptune/dist/install/sea/*.exe
          if-no-files-found: warn
          retention-days: 30

      # ── 11. Upload zip (fallback) ──────────────────────────────────────────
      - name: Upload Neptune zip
        uses: actions/upload-artifact@v4
        with:
          name: Neptune-Windows-Zip
          path: obj-neptune/dist/*.zip
          if-no-files-found: warn
          retention-days: 30
